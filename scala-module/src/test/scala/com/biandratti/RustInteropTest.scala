package com.biandratti

import munit.FunSuite

class RustInteropTest extends FunSuite {

  test("multiply_by_two called") {
    assert(false)
    // assert(RustInterop.multiply_by_two(5) == 11)
  }
}
